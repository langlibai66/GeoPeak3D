# Assets

Place public-facing media here before publishing the repository:

- `demo.mp4`: qualitative localization video;
- `demo-cover.svg`: video cover shown in the README;
- `network-architecture.png`: README preview of the network architecture;
- `network-architecture.pdf`: original vector architecture figure.

After uploading `demo.mp4`, replace the placeholder link in the root README
with the GitHub-hosted video URL (dragging the file into a GitHub issue or the
README editor generates a browser-playable URL).
